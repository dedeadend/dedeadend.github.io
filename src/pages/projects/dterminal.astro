---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { projectDetails, projects } from '../../data/site';
const project = projects.find((item) => item.slug === 'dterminal')!;
const detail = projectDetails.dterminal;
---
<BaseLayout title="DTerminal" description={detail.summary}>
  <div class="container page">
    <p><a class="back-link" href="/projects/">← Projects</a></p>
    <header class="page-header project-detail-header"><span class="eyebrow">{detail.eyebrow}</span><h1>{detail.title}</h1><p>{detail.summary}</p><div class="actions"><a class="button primary" href={project.github}>View on GitHub</a></div></header>
    <section class="detail-intro"><div class="detail-stat"><strong>Android</strong><span>Platform</span></div><div class="detail-stat"><strong>Python 3.13</strong><span>Embedded runtime</span></div><div class="detail-stat"><strong>Clean + MVI</strong><span>Architecture</span></div></section>
    <section class="section prose"><span class="eyebrow">Overview</span><h2>A terminal designed for Android.</h2><p>{detail.overview}</p></section>
    <section class="section prose"><span class="eyebrow">The problem</span><h2>More than a command prompt.</h2><p>{detail.problem}</p></section>
    <section class="section"><span class="eyebrow">Approach</span><h2>Built around a real runtime.</h2><div class="grid">{detail.approach.map(([title, text]) => <article class="card"><h3>{title}</h3><p>{text}</p></article>)}</div></section>
    <section class="section prose"><span class="eyebrow">Architecture</span><h2>State stays predictable.</h2><p>{detail.architecture}</p></section>
    <section class="section prose"><span class="eyebrow">System integration</span><h2>Close to the shell.</h2><p>{detail.system}</p></section>
    <section class="section prose"><span class="eyebrow">Design decisions</span><h2>Power without desktop baggage.</h2><p>The project treats Android as its native environment rather than trying to reproduce a desktop terminal one-to-one. Persistent state, touch-first controls, runtime management, and asynchronous execution are considered together.</p></section>
    <section class="section prose"><span class="eyebrow">What's next</span><h2>Keep the runtime useful.</h2><p>{detail.next}</p></section>
    <section class="section"><div class="actions"><a class="button primary" href={project.github}>Explore the source</a><a class="button" href="/projects/">Back to projects</a></div></section>
  </div>
</BaseLayout>
